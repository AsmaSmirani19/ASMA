syntax = "proto3";

package testpb;

option go_package = "./testpb";

message TestCommand {
  string test_id = 1;
  string parameters = 2;
}

message TestResponse {
  string status = 1;
  string result = 2;
}

service TestService {
  rpc StreamTestCommands(TestCommand) returns (stream TestResponse);
}